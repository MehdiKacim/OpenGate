{
  "name": "opengate",
  "version": "0.0.13",
  "license": "MIT",
  "type": "module",
  "bin": {
    "opengate": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts serve",
    "auth": "bun run src/cli.ts auth login",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gpt-tokenizer": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
